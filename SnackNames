#List of names
names = ["Kim", "Ellie", "Alex"]
snacks = [ ]

index = 0

#print names and length of them for loop
for name in names:
	snack = input(name + ", what is your favorite snack?")
	snacks.append(snack)
	name_length = len(name)
	print("Your name is " + str(name_length) + " characters long.")

for name in names
	print(name + " your fave snack is " + snacks [index] + ".")
	index = index + 1
